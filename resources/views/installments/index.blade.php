@extends('layouts.elite-mobile-marketplace')
@section('content')<x-public.public-header /><main class="mx-auto max-w-4xl px-4 py-8"><h1 class="text-2xl font-bold">My Installment Applications / طلبات التقسيط</h1><div class="mt-5 space-y-3">@forelse($applications as $application)<a class="pm-card block" href="{{ route('installments.show',$application) }}"><div class="flex justify-between gap-3"><strong>{{ $application->application_number }}</strong><span>{{ str_replace('_',' ', $application->status) }}</span></div><p class="mt-2 text-sm text-slate-600">{{ $application->product_name_snapshot }} · {{ $application->monthly_payment }} {{ $application->currency }}/month</p></a>@empty<p class="pm-card">No applications yet.</p>@endforelse</div><div class="mt-5">{{ $applications->links() }}</div></main>@endsection
